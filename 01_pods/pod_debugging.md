# [보충 강의] 파드(Pod) 디버깅 하는 방법

개발을 하다 보면 에러를 디버깅하고 해결하는 데 대부분의 시간을 쓴다. 따라서 어떤 기술을 익힐 때 반드시 에러를 디버깅할 수 있는 방법을 정리해두어야 한다.

---

## ✅ 파드(Pod)가 정상적으로 실행되지 않았을 때

파드를 생성하다 보면 `STATUS`가 `ErrImagePull`, `ImagePullBackOff`, `CrashLoopBackOff` 등으로 표시되며 정상적으로 실행되지 않는 경우가 있다. 이때 구체적인 원인을 파악하는 방법을 알아본다.

#### 1. 에러 상황 재현 (매니페스트 파일)
이미지 태그를 잘못 기재하여 에러를 유도해 본다.

**nginx-pod-error.yaml**
```yaml
apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod-error
spec:
  containers:
    - name: nginx-container
      image: nginx:1.26.4 # 존재하지 않는 버전을 입력하여 에러 유도
      ports:
        - containerPort: 80
```

#### 2. 파드 생성 및 조회
```bash
$ kubectl apply -f nginx-pod-error.yaml
$ kubectl get pods
```

**출력 결과:**
```text
NAME              READY   STATUS         RESTARTS   AGE
nginx-pod-error   0/1     ErrImagePull   0          10s
```
`STATUS`만 봐서는 이미지를 가져오는 데 실패했다는 것만 알 수 있고, 구체적인 이유는 알 수 없다.

#### 3. 상세 에러 메시지 확인하기 (`describe`)
`kubectl describe` 명령어를 사용하면 파드의 상태 변화 기록(Events)을 포함한 상세 정보를 볼 수 있다.

```bash
# kubectl describe pods [파드명]
$ kubectl describe pods nginx-pod-error
```

**확인해야 할 부분 (Events):**
상세 정보 하단의 `Events` 섹션을 보면 왜 에러가 발생했는지 구체적으로 적혀 있다.
- `Failed to pull image "nginx:1.26.4": ... manifest for nginx:1.26.4 not found` 와 같은 메시지를 통해 버전이 잘못되었음을 확신할 수 있다.

---

## ✅ 파드(Pod)의 로그를 확인하고 싶을 때

파드가 `Running` 상태임에도 불구하고 애플리케이션 내부에서 에러가 발생하거나, 요청이 어떻게 처리되는지 보고 싶을 때는 로그를 확인해야 한다.

#### 1. 파드 로그 확인하기 (`logs`)
```bash
# kubectl logs [파드명]
$ kubectl logs nginx-pod
```

#### 2. 실시간 로그 확인하기 (Option)
터미널을 띄워놓고 로그가 쌓이는 것을 실시간으로 보고 싶다면 `-f` (follow) 옵션을 사용한다.
```bash
$ kubectl logs -f nginx-pod
```

---

## ✅ 파드(Pod)에 접속하고 싶을 때

애플리케이션 설정 파일이 잘 들어갔는지, 혹은 컨테이너 내부 네트워크 상태가 어떤지 직접 확인해야 할 때 파드 내부로 접속할 수 있다.

```bash
# kubectl exec -it [파드명] -- bash
$ kubectl exec -it nginx-pod -- bash

# 만약 bash가 없다면 sh로 시도
$ kubectl exec -it nginx-pod -- sh
```

- **도커와 비슷함**: `docker exec -it [컨테이너ID] bash` 명령어와 유사하다.
- **주의사항**: 컨테이너 이미지 종류(Alpine 등)에 따라 `bash`가 설치되어 있지 않을 수 있다. 이 경우 에러가 발생하면 `sh`로 접속을 시도한다.

---

### 💡 요약: 디버깅 3단계 전략

1.  **`kubectl get pods`**: 전체적인 상태(Status)를 먼저 확인한다.
2.  **`kubectl describe pod [명]`**: 파드가 왜 안 뜨는지(Events) 상세 원인을 분석한다.
3.  **`kubectl logs [명]`**: 파드는 떴는데 내부 로직이 문제일 때 로그를 확인한다.
