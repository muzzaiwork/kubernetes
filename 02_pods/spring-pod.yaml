apiVersion: v1
kind: Pod
metadata:
  name: spring-pod
spec:
  containers:
    - name: spring-container
      image: spring-server
      imagePullPolicy: IfNotPresent   # 로컬 이미지가 있으면 사용, 없으면 원격에서 Pull
      ports:
        - containerPort: 8080
